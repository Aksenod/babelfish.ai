# Переводчик речи EN → RU

Онлайн-приложение для перевода английской речи в русскую в реальном времени. Распознавание — OpenAI Whisper, перевод — Яндекс.Переводчик. В продакшене ключи живут на Vercel, фронт на GitHub Pages работает без ввода ключей.

## Возможности

- 🎤 Распознавание английской речи в реальном времени (OpenAI Whisper API)
- 🔄 Автоматический перевод на русский язык (Яндекс.Переводчик API)
- 📝 Отображение оригинала и перевода одновременно
- ⚙️ Опциональная настройка API ключей через интерфейс
- 💾 Сохранение настроек в localStorage

## Требования

- Современный браузер с поддержкой MediaRecorder API
- Для режима "без настройки": сервер на Vercel с переменными окружения
- Для режима "со своими ключами": OpenAI API key и Яндекс.Переводчик API key

## Настройка

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка Vercel (для режима без ключей)

Создайте проект на Vercel и добавьте переменные окружения:

```env
OPENAI_API_KEY=...
YANDEX_API_KEY=...
ALLOWED_ORIGINS=https://aksenod.github.io,https://aksenod.github.io/babelfish.ai
```

Далее используйте URL проекта в переменных сборки фронтенда:

```env
VITE_YANDEX_PROXY_URL=https://babelfish-ai.vercel.app/api/yandex-translate
VITE_OPENAI_TRANSCRIBE_URL=https://babelfish-ai.vercel.app/api/openai-transcribe
VITE_OPENAI_SUMMARY_URL=https://babelfish-ai.vercel.app/api/openai-summary
```

### 3. Получение API ключей (опционально)

**OpenAI API ключ:**
1. Перейдите на [platform.openai.com/api-keys](https://platform.openai.com/api-keys)
2. Создайте аккаунт (если нет)
3. Создайте новый API ключ
4. Скопируйте ключ (начинается с `sk-`)

**Яндекс.Переводчик API ключ:**
1. Перейдите на [console.cloud.yandex.ru](https://console.cloud.yandex.ru/)
2. Зарегистрируйтесь или войдите в аккаунт
3. Создайте платежный аккаунт (привяжите карту)
4. Создайте сервисный аккаунт
5. Назначьте роль `yc.ai.translate.execute`
6. Создайте API ключ в разделе "Ключи доступа"
7. Скопируйте секретный ключ (начинается с `AQVN...`)

### 4. Настройка ключей в приложении (опционально)

1. Запустите приложение:
   ```bash
   npm run dev
   ```

2. Откройте приложение в браузере

3. Нажмите кнопку "⚙️ Настройки" в правом верхнем углу

4. Введите ваши API ключи:
   - OpenAI API Key
   - Яндекс.Переводчик API Key

5. Нажмите "Сохранить"

Ключи будут сохранены в localStorage браузера и автоматически использоваться при следующем запуске.
Если ключи не указаны, запросы идут через Vercel и используют серверные ключи.

## Использование

1. Откройте приложение в браузере
2. Разрешите доступ к микрофону при запросе браузера
3. Говорите на английском языке
4. Приложение автоматически:
   - Записывает аудио (по 4 секунды)
   - Отправляет на распознавание (OpenAI Whisper)
  - Переводит текст (Яндекс.Переводчик)
   - Отображает результат (английский текст + русский перевод)

## Стоимость использования

**OpenAI Whisper API:**
- $0.006 за минуту аудио
- Примерно $0.36 за час записи

**Яндекс.Переводчик API:**
- 1,000,000 символов в день бесплатно
- До 10,000,000 символов в месяц бесплатно
- Для большинства случаев этого достаточно
- Платные тарифы доступны после превышения лимита

## Разработка

```bash
# Запуск dev сервера
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр продакшен сборки
npm run preview
```

## Технологии

- React 18
- Vite
- TailwindCSS
- OpenAI Whisper API
- Яндекс.Переводчик API

## Лицензия

MIT
